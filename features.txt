0)  Uses Embedded App SDK (EASDK) to embed apps directly inside of the Shopify admin and provide access to native admin GUI features.
1)  CORS middleware
2)  Uninstall webhook
3)  Register script tags to inject html / css /javascript to manipulate storefront
4)  Create application charge and activate recurring charge
5)  App Installation
5a) Create a session and request shop access token and store in DB for later use when making api calls to the store
5b) Request permissions for application scope
6)  HMAC validation
7)  Gracefully fallback when user declines recurring charge
8)  Celery task to periodically check if recurring charge is still active (if not store is deleted from app server "Registered Store" Database and     app service is no longer provided)

